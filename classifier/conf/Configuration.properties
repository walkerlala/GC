# sourceDB setting
sourceDBUrl=jdbc:mysql://127.0.0.1:3306/
sourceDBUser=root
sourceDBPasswd=root0717
sourceDBName=crawlerDB

# trainingDB setting
trainingDBUrl=jdbc:mysql://127.0.0.1:3306/
trainingDBUser=root
trainingDBPasswd=root0717
trainingDBName=trainingDB

# tfidfDB setting
tfidfDBUrl=jdbc:mysql://127.0.0.1:3306/
tfidfDBUser=root
tfidfDBPasswd=root0717
tfidfDBName=tfidfDB

# resultDB setting
targetDBUrl=jdbc:mysql://127.0.0.1:3306/
targetDBUser=root
targetDBPasswd=root0717
targetDBName=resultDB

# classifier name.( "knn", ...)
# when implemented one more classifier, add its name above
classifier=knn
# K in KNN classifycation algorithm
# too little data, so we have to make it just 1 to utilize the KNN score
K=1
# We use `keywords' and `description' in HTML tag to classify.
# We assign different weight to those two features
KNNKeywordsWeight=0.45
KNNTitleWeight=0.4
KNNDescriptionWeight=0.25
# number of tags
numOfTag=13
# tags
tags=新闻:1;小说:2;购物:3;医疗:4;视频:5;社交:6;游戏:7;旅游:8;服务:9;网络课程:10;音乐:11;软件下载:12;无类别:13
